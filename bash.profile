# Add the following to your .bash_profile - or some appropriate
# reconstruction to suit you use of the components

export DSERVER_HOME=/path/to/dserver/repo

export PATH=${PATH}:${DSERVER_HOME}/bin

export PYTHONPATH=${DSERVER_HOME}/lib/py

export DSERVER_DIR=${DSERVER_HOME}/DATA

